<configuration>

    <property name="LOG_DIR" value="${LOG_DIR:-./logs}" />
    <property name="ROOT_LEVEL" value="${ROOT_LEVEL:-INFO}" />
    <property name="CONSOLE_LEVEL" value="${CONSOLE_LEVEL:-OFF}" />
    <property name="FILE_LEVEL" value="${FILE_LEVEL:-INFO}" />

    <!--
        %logger{0}  = short class name only:       TelemetryLifecycleRouter
        %logger{20} = abbreviated package + class:  c.p.a.t.i.TelemetryLifecycleRouter
        %logger{50} = full path (less truncation)
        %thread     = thread/coroutine name — essential for async debugging
    -->
    <property name="CONSOLE_PATTERN"
              value="%d{HH:mm:ss.SSS} %-5level [%thread] %logger{0}.%method - %msg%n"/>
    <property name="FILE_PATTERN"
              value="%date %-5level [%thread] %logger{20} - %msg%n"/>

    <property name="MAX_FILE_SIZE" value="${MAX_FILE_SIZE:-2MB}" />
    <property name="MAX_HISTORY" value="${MAX_HISTORY:-3}" />
    <property name="TOTAL_SIZE_CAP" value="${TOTAL_SIZE_CAP:-30MB}" />

    <turboFilter class="com.project.analyzer.utils.logger.RateLimitingFilter">
        <markerName>RATE_LIMITED</markerName>
        <frequency>240</frequency>
    </turboFilter>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>${CONSOLE_LEVEL}</level>
        </filter>
        <encoder>
            <pattern>${CONSOLE_PATTERN}</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/app.log</file>

        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>${FILE_LEVEL}</level>
        </filter>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/app.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>${MAX_FILE_SIZE}</maxFileSize>
            <maxHistory>${MAX_HISTORY}</maxHistory>
            <totalSizeCap>${TOTAL_SIZE_CAP}</totalSizeCap>
            <cleanHistoryOnStart>true</cleanHistoryOnStart>
        </rollingPolicy>

        <encoder>
            <pattern>${FILE_PATTERN}</pattern>
        </encoder>
    </appender>

    <root level="${ROOT_LEVEL}">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>

</configuration>
